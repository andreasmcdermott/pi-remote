{
  "name": "pi-remote",
  "version": "0.1.0",
  "description": "Mobile remote for the pi coding agent",
  "type": "module",
  "scripts": {
    "start": "bun run bridge.ts",
    "dev": "bun --watch bridge.ts"
  },
  "dependencies": {
    "@mariozechner/pi-coding-agent": "*",
    "web-push": "^3.6.7"
  }
}
